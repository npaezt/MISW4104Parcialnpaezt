export interface Car {
      id: number;
      linea: string;
      modelo: string;
      marca: string;  
      referencia: string;
      kilometraje: number;
      color: string;
      imagen: string;
}